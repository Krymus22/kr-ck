{
  "$schema": "../../../mcps/schema.json",
  "name": "Roblox_Studio",
  "description": "Official Roblox Studio MCP server (built into Studio). Enables the IA to read the game data model, inspect instances, execute Luau code, run playtests, and capture screen. WRITE operations (multi_edit, generate_*, insert_from_creator_store) are blocked by robloxMcpGuard — the IA must use aplicar_diff instead, which goes through Bug Hunter + DataGuard + read-before-write + rollback.",
  "transport": "stdio",
  "command": "cmd.exe",
  "args": ["/c", "%LOCALAPPDATA%\\Roblox\\mcp.bat"],
  "env": {},
  "autoStart": true,
  "platformOverrides": {
    "darwin": {
      "command": "/Applications/RobloxStudio.app/Contents/MacOS/StudioMCP",
      "args": []
    },
    "linux": {
      "_comment": "Roblox Studio MCP is not available on Linux. The server will be skipped at startup with a clear warning (see extensions.ts startAndInitMCPServer platform guard). Set 'command' to a real Linux binary if you have a custom build of StudioMCP for Linux.",
      "command": "true",
      "args": []
    }
  },
  "notes": [
    "This MCP server is BUILT INTO Roblox Studio (no separate install needed).",
    "The user must enable it in Studio: Assistant > ... > Manage MCP Servers > Enable.",
    "Windows: uses cmd.exe /c %LOCALAPPDATA%\\Roblox\\mcp.bat",
    "macOS: uses /Applications/RobloxStudio.app/Contents/MacOS/StudioMCP",
    "Linux: NOT available — Roblox Studio does not run on Linux. The server is skipped at startup with a warning.",
    "Claude-Killer auto-starts the MCP server when mode Roblox is active (Windows/macOS only).",
    "WRITE tools are blocked by robloxMcpGuard.ts for safety.",
    "See: https://create.roblox.com/docs/studio/mcp"
  ]
}
