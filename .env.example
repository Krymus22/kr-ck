# ─── Required: NVIDIA NIM API ───────────────────────────────────────────────
# Set ONE of the following (at least one is required):

# Option A: Single key (40 RPM, 1 concurrent — original behavior)
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option B: Multi-key pool (N × 40 RPM, N concurrent — for parallel sub-agents)
# Comma-separated list of NVIDIA API keys
# NVIDIA_API_KEYS=nvapi-key1,nvapi-key2,nvapi-key3

# Option C: Keys from file (one per line)
# NVIDIA_API_KEYS_FILE=/path/to/keys.txt

# ─── Model ─────────────────────────────────────────────────────────────────
# Model identifier on NVIDIA NIM
# Options: moonshotai/kimi-k2.6, minimaxai/minimax-m3, meta/llama-3.1-405b-instruct, etc
# MODEL=moonshotai/kimi-k2.6

# Sampling parameters (NVIDIA recommended defaults)
# TEMPERATURE=1.0
# TOP_P=0.95
# MAX_TOKENS=16384

# ─── Effort Level (IDEIA 4) ───────────────────────────────────────────────
# Controls reasoning depth via system prompt (low|medium|high|max)
# low = fast, skip self-validation and auto-tests
# medium = default, balanced
# high = think before every write, enable sub-agents
# max = maximum reasoning, validate before finish_reason
# CLAUDE_KILLER_EFFORT=medium

# ─── Strict Quality Gate (IDEIA 3.4) ───────────────────────────────────────
# When true, blocks finish_reason until tsc + lint pass
# STRICT_MODE=true
# STRICT_GATE_TSC=true
# STRICT_GATE_LINT=true
# STRICT_GATE_MAX_BLOCKS=8
# STRICT_GATE_SKIP_PATTERNS=

# ─── LSP Integration (IDEIA 3.10) ──────────────────────────────────────────
# LSP_ENABLED=true
# LSP_TSSERVER_PATH=
# LSP_PYLSP_PATH=
# LSP_REQUEST_TIMEOUT_MS=5000

# ─── Rate Limiting ─────────────────────────────────────────────────────────
# RATE_LIMIT_RPM=40
# MAX_CONCURRENCY=1
# MAX_HEAL_RETRIES=3

# ─── Context Window ────────────────────────────────────────────────────────
# CONTEXT_WINDOW_TOKENS=128000
# CONTEXT_COMPACT_THRESHOLD=0.75
# CONTEXT_WARN_THRESHOLD=0.6

# ─── Diff Preview ──────────────────────────────────────────────────────────
# DIFF_PREVIEW=true

# ─── Debug ─────────────────────────────────────────────────────────────────
# DEBUG=false
